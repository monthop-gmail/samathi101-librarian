ด้านล่างนี้คือการรวบรวมสิ่งที่คุยกันทั้งหมด ทำเป็นโครงสร้าง Markdown สำหรับเอาไปใช้ใน GitHub/คู่มือทีมงานได้เลยครับ [gemini.google](https://gemini.google.com/share/8755303f0adc)

***

# ระบบ Gemini RAG สำหรับแบบสอบถามและหลักสูตรสมาธิ

เอกสารนี้สรุปแนวคิด โครงสร้างโฟลเดอร์ เวิร์กโฟลว์ GitHub Actions และสคริปต์ที่เกี่ยวข้องกับระบบ “AI Librarian” เพื่อจัดการเอกสารหลักสูตรและแบบสอบถามของสถาบันพลังจิตตานุภาพด้วย Gemini RAG [gemini.google](https://gemini.google.com/share/8755303f0adc)

***

## แนวคิดการใช้ Gemini RAG กับระบบแบบสอบถาม

- ใช้ RAG เพื่อทำให้แบบสอบถาม “ฉลาด” ขึ้น ไม่ใช่แค่ดึงข้อมูลมาตอบ แต่ช่วยสร้างคำถาม วิเคราะห์ และประเมินผล [gemini.google](https://gemini.google.com/share/8755303f0adc)
- มิติหลักของการประยุกต์ใช้กับแบบสอบถาม: การสร้างแบบสอบถามแบบไดนามิก, ผู้ช่วยตอบแบบสอบถาม, และการวิเคราะห์คำตอบเชิงคุณภาพ (open-ended) [gemini.google](https://gemini.google.com/share/8755303f0adc)
- กรณีสถาบันพลังจิตตานุภาพ: ใช้กับแบบสอบถามระหว่างเรียน, ก่อน/หลังสอบภาคสนามธุดงค์ และการติดตามพัฒนาการรายบุคคล [gemini.google](https://gemini.google.com/share/8755303f0adc)

***

## โครงสร้างข้อมูล RAG (Knowledge Base + Survey Data)

### 1. ประเภทข้อมูลหลัก

- Static Data (องค์ความรู้):  
  - คู่มือหลักสูตรครูสมาธิ เล่ม 1–3, อาจาริยสาสมาธิ, วิทันตสาสมาธิ ฯลฯ [gemini.google](https://gemini.google.com/share/8755303f0adc)
  - กฎ ระเบียบ และคู่มือการสอบภาคสนามธุดงค์ [gemini.google](https://gemini.google.com/share/8755303f0adc)
- Dynamic Data (คำตอบแบบสอบถาม):  
  - ไฟล์ CSV เช่น `WatPaRoiPee-2025.csv` และข้อมูลจาก Google Forms/KoboToolbox [gemini.google](https://gemini.google.com/share/8755303f0adc)

### 2. โครงสร้างโฟลเดอร์หลัก (Project Structure)

```plaintext
root-folder/
├── 00_INBOX_UNPROCESSED/        # พื้นที่รับไฟล์ดิบ (ยังไม่จัดหมวด)
├── 01_Curriculum_Master_Data/   # Static Knowledge: คู่มือหลักสูตร
│   ├── Level_1_Foundation/
│   ├── Level_2_Intermediate/
│   └── Level_3_Advanced/
├── 02_Survey_Data/              # Dynamic Data: แบบสอบถาม/ผลประเมิน
│   ├── By_Course/
│   └── By_Event/
│       └── Thudong_Field_Exam/
│           └── 2025-12_WatPaRoiPee/
├── 03_RAG_Assets/               # Embeddings, Prompt templates ฯลฯ
│   ├── Embeddings/
│   └── Prompts_Templates/
└── 04_Reports_Insights/         # รายงานสรุปจาก Gemini
    ├── Executive_Summaries/
    └── SWOT_Analysis/
```



***

## การจัดโครงสร้างหลักสูตร 50 หลักสูตร

### ตัวอย่างกลุ่มหลักสูตร (บางส่วน)

```python
COURSES_CONTEXT = """
จำแนกไฟล์เข้าตามกลุ่มหลักสูตรของสถาบันพลังจิตตานุภาพ ดังนี้:

1. กลุ่มหลักสูตรพื้นฐาน (Foundation):
- WP-01: หลักสูตรครูสมาธิ (หลักสูตรหลัก 6 เดือน)
- WP-02: หลักสูตรวิทันตสาสมาธิ
- WP-03: หลักสูตรอัตตาสาสมาธิ
- WP-04: หลักสูตรชินนสาสมาธิ
- WP-05: หลักสูตรนิรสาสมาธิ

2. กลุ่มหลักสูตรขั้นสูง (Advanced):
- WP-10: หลักสูตรอาจาริยสาสมาธิ
- WP-11: หลักสูตรญาณสาสมาธิ
- WP-12: หลักสูตรอุตตมสาสมาธิ

3. กลุ่มหลักสูตรพิเศษและกิจกรรม:
- WP-99: การสอบภาคสนามธุดงค์
- WP-EX: หลักสูตรสมาธิออนไลน์ (Willpower Online)
- WP-CHILD: หลักสูตรสมาธิเด็กและเยาวชน
"""
```



แนะนำให้เก็บ Master List ให้ครบ 50 หลักสูตรในไฟล์เดียว เช่น `courses_list.json` เพื่อใช้เป็น Master Data ให้ AI อ้างอิง [gemini.google](https://gemini.google.com/share/8755303f0adc)

***

## AI Librarian: แนวคิดและเวิร์กโฟลว์

### แนวคิด

- มีโฟลเดอร์ `00_INBOX_UNPROCESSED` เป็น “กล่องรับเอกสาร” ที่ทุกคนสามารถโยนไฟล์เข้ามาได้ [gemini.google](https://gemini.google.com/share/8755303f0adc)
- ใช้ Gemini วิเคราะห์ไฟล์แต่ละไฟล์ เพื่อ:
  - จำแนกหลักสูตร (course_id) และประเภทเอกสาร (คู่มือ/แบบสอบถาม/ข้อสอบ) [gemini.google](https://gemini.google.com/share/8755303f0adc)
  - แนะนำชื่อไฟล์มาตรฐาน [ID]_[Type]_[Year].ext [gemini.google](https://gemini.google.com/share/8755303f0adc)
  - ระบุโฟลเดอร์ปลายทางและสร้าง `metadata.json` คู่กับเอกสาร [gemini.google](https://gemini.google.com/share/8755303f0adc)

### ตัวอย่าง metadata มาตรฐาน

```json
{
  "original_filename": "คู่มือครูสมาธิ_เล่ม1_final.pdf",
  "assigned_course": "หลักสูตรครูสมาธิ (WP-01)",
  "category": "Curriculum_Manual",
  "level": "Intermediate",
  "version_year": "2567",
  "status": "Processed",
  "missing_info": [
    "ยังขาดวันที่อนุมัติหลักสูตรฉบับปรับปรุง",
    "ยังไม่มีรายชื่อผู้จัดทำฝ่ายวิชาการ"
  ]
}
```



***

## GitHub Actions Workflow (AI Librarian Pipeline)

### 1. ไฟล์ Workflow: `.github/workflows/ai-librarian.yml`

```yaml
name: AI Librarian - Auto File Organizer

on:
  push:
    paths:
      - '00_INBOX_UNPROCESSED/**'

jobs:
  organize-files:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          pip install google-generativeai pymupdf4llm python-frontmatter

      - name: Process Files with Gemini
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          python scripts/organize_files.py

      - name: Commit and Push Changes
        run: |
          git config --global user.name "Gemini-Librarian-Bot"
          git config --global user.email "bot@gemini.ai"
          git add .
          git commit -m "AI: Organized files and generated metadata [skip ci]" || echo "No changes to commit"
          git push
```



### 2. การตั้งค่า Secrets ที่จำเป็น

- `GEMINI_API_KEY`: API key จาก Google AI Studio หรือ Vertex AI [docs.cloud.google](https://docs.cloud.google.com/vertex-ai/generative-ai/docs/start/quickstart)

***

## สคริปต์หลัก: `scripts/organize_files.py`

> หมายเหตุ: ด้านล่างนี้คือโครง/โครงสร้างหลัก สามารถเติม logic รายละเอียดเพิ่มได้ตามที่ทีมจะ implement จริง [dev](https://dev.to/m_sea_bass/how-to-convert-pdfs-to-markdown-using-pymupdf4llm-and-its-evaluation-kg6)

```python
import os
import shutil
import json
import google.generativeai as genai
import pymupdf4llm

# 1. ตั้งค่า Gemini
genai.configure(api_key=os.environ["GEMINI_API_KEY"])
model = genai.GenerativeModel("gemini-1.5-pro")

INBOX_DIR = "00_INBOX_UNPROCESSED"
MASTER_DIR = "01_Curriculum_Master_Data"
SURVEY_DIR = "02_Survey_Data"

COURSES_CONTEXT = """
ID: WP-01 | Name: หลักสูตรครูสมาธิ
ID: WP-02 | Name: หลักสูตรวิทันตสาสมาธิ
ID: WP-99 | Name: การสอบภาคสนามธุดงค์
(เพิ่มให้ครบ 50 หลักสูตรในภายหลัง)
"""

def convert_pdf_to_md(target_path: str) -> None:
    """แปลง PDF เป็น Markdown วางไฟล์ .md คู่กับ .pdf เดิม"""
    try:
        md_text = pymupdf4llm.to_markdown(target_path)
        md_path = target_path.replace(".pdf", ".md")
        with open(md_path, "w", encoding="utf-8") as f:
            f.write(md_text)
    except Exception as e:
        print(f"Error converting {target_path} to markdown: {e}")

def process_file(file_path: str) -> None:
    if not os.path.isfile(file_path):
        return

    filename = os.path.basename(file_path)
    if filename == ".gitkeep":
        return

    prompt = f"""
คุณคือบรรณารักษ์ดิจิทัลของสถาบันพลังจิตตานุภาพ

ข้อมูลหลักสูตร:
{COURSES_CONTEXT}

ไฟล์ชื่อ: "{filename}"

งานของคุณ:
1. ระบุรหัสหลักสูตร (course_id) และชื่อหลักสูตร (course_name)
2. ระบุประเภทเอกสาร (Manual, Survey, Exam, Other)
3. ระบุปี (year) ถ้าอนุมานได้
4. เสนอชื่อไฟล์ใหม่เป็นมาตรฐาน: [course_id]_[type]_[year].ext
5. ระบุโฟลเดอร์ปลายทาง เป็น path ที่สัมพันธ์กับ root:
   - ถ้าเป็นคู่มือ/ตำรา ให้ใช้ภายใต้ "{MASTER_DIR}"
   - ถ้าเป็นแบบสอบถาม/ผลประเมิน ให้ใช้ภายใต้ "{SURVEY_DIR}"

ตอบกลับเป็น JSON เท่านั้นในรูปแบบ:
{{
  "target_dir": "01_Curriculum_Master_Data/WP-01",
  "new_filename": "WP-01_Manual_2568.pdf",
  "metadata": {{
    "course_id": "WP-01",
    "course_name": "หลักสูตรครูสมาธิ",
    "doc_type": "Manual",
    "year": "2568",
    "missing_info": []
  }}
}}
"""

    response = model.generate_content(prompt)
    raw_text = response.text.strip()
    clean_text = raw_text.replace("```json", "").replace("```", "")
    result = json.loads(clean_text)

    target_dir = result["target_dir"]
    new_filename = result["new_filename"]
    target_path = os.path.join(target_dir, new_filename)

    os.makedirs(target_dir, exist_ok=True)
    shutil.move(file_path, target_path)

    # แปลง PDF เป็น Markdown ถ้าเป็นคู่มือ .pdf
    if target_path.lower().endswith(".pdf"):
        convert_pdf_to_md(target_path)

    meta_path = target_path + ".json"
    with open(meta_path, "w", encoding="utf-8") as f:
        json.dump(result["metadata"], f, ensure_ascii=False, indent=2)

def process_files() -> None:
    if not os.path.exists(INBOX_DIR):
        return
    for fname in os.listdir(INBOX_DIR):
        fpath = os.path.join(INBOX_DIR, fname)
        process_file(fpath)

if __name__ == "__main__":
    process_files()
```



***

## Dashboard บน README.md

ตัวอย่างตาราง Inventory ที่หน้า `README.md` เพื่อให้ผู้บริหารและทีมงานเห็นภาพรวมว่าแต่ละหลักสูตรมีข้อมูลพร้อมทำ RAG แค่ไหน [gemini.google](https://gemini.google.com/share/8755303f0adc)

```markdown
## 🏆 Meditation Course Data Inventory

| ID    | หลักสูตร                 | คู่มือ (Manuals) | แบบสอบถาม (Surveys) | สถานะความพร้อม (RAG Ready) |
| :---- | :----------------------- | :---------------: | :------------------: | :---------------------------: |
| WP-01 | ครูสมาธิ                | 3                 | 12                   | ⚡ พร้อมใช้งาน               |
| WP-02 | วิทันตสาสมาธิ          | 1                 | 0                    | 🟡 ข้อมูลไม่ครบ              |
| WP-99 | สอบภาคสนามธุดงค์       | 1                 | 850                  | ⚡ พร้อมใช้งาน               |
```

(จำนวนจริงสามารถให ้สคริปต์นับและเขียนลง README โดยอัตโนมัติในฟังก์ชัน `update_dashboard()` ที่ทีมงานจะเติมในภายหลัง) [gemini.google](https://gemini.google.com/share/8755303f0adc)

***

## แนวปฏิบัติเรื่อง PDF → Markdown

- เอกสารหลักสูตร (Manual) แนะนำให้เก็บทั้ง `.pdf` (ต้นฉบับ) และ `.md` (สำหรับ AI/RAG) ในโฟลเดอร์เดียวกัน [dev](https://dev.to/m_sea_bass/how-to-convert-pdfs-to-markdown-using-pymupdf4llm-and-its-evaluation-kg6)
- ใช้ `pymupdf4llm.to_markdown()` เพื่อแปลง PDF เป็น Markdown ที่เหมาะกับ LLM (รองรับโครงสร้างหัวข้อและตารางได้ดี) [artifex](https://artifex.com/blog/building-a-multimodal-llm-application-with-pymupdf4llm)
- แบบสอบถามที่เป็น CSV/Excel ไม่จำเป็นต้องแปลงเป็น Markdown เพราะ AI อ่านข้อมูลเชิงตารางได้โดยตรง [gemini.google](https://gemini.google.com/share/8755303f0adc)

***

ถ้าต้องการ ผมสามารถช่วยจัดโครง “README ฉบับทีมงาน” แยกอีกไฟล์หนึ่งสำหรับอธิบายขั้นตอนแบบสั้นมาก (หน้าเดียว) ให้ใช้สอนทีมสาขาได้ด้วยครับ [gemini.google](https://gemini.google.com/share/8755303f0adc)