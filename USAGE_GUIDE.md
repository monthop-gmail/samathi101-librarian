# คู่มือการใช้งาน AI Librarian (Usage Guide)

ระบบจัดการเอกสารหลักสูตรอัตโนมัติด้วย AI (Gemini 2.0 Flash)

---

## 🚀 การเริ่มต้นใช้งาน (Setup)

### 1. ความต้องการของระบบ (Prerequisites)
- **Windows** ที่มี **WSL (Debian)** ติดตั้งไว้
- **Docker Desktop** (และเปิดใช้งาน WSL Integration)
- **Gemini API Key** (สำหรับใช้งาน AI Classification)

### 2. การตั้งค่า Environment
สร้างไฟล์ชื่อ `.env` ในโฟลเดอร์รหัสโปรเจกต์ (ถ้ายังไม่มี) และใส่ API Key:
```env
GEMINI_API_KEY=your_api_key_here
```

---

## 📁 โครงสร้างโฟลเดอร์ (Folder Structure)

| โฟลเดอร์ | หน้าที่ |
| :--- | :--- |
| `00_INBOX_UNPROCESSED` | **(Inbox)** สำหรับวางไฟล์ที่ต้องการให้ AI ประมวลผล |
| `01_Curriculum_Master_Data` | เก็บไฟล์ **Manual (คู่มือ)** และ **Exam (ข้อสอบ)** ที่จัดระเบียบแล้ว |
| `02_Survey_Data` | เก็บไฟล์ **Survey / Eval (แบบประเมิน)** ที่จัดระเบียบแล้ว |
| `scripts/` | เก็บสคริปต์หลักทางเทคนิค |

---

## 🛠️ วิธีใช้งาน (How to Use)

1. **ส่งไฟล์เข้า Inbox**: นำไฟล์ PDF หรือ CSV ไปวางใน `00_INBOX_UNPROCESSED/`
2. **รันระบบ**: เปิด Terminal ในโฟลเดอร์โปรเจกต์แล้วรันคำสั่ง:
   ```bash
   # หากรันผ่าน WSL
   docker compose up librarian
   ```
3. **ตรวจสอบผลลัพธ์**: 
   - ไฟล์จะถูกย้ายไปยังโฟลเดอร์ที่ถูกต้องตามประเภทข้อมูล
   - ไฟล์จะถูกเปลี่ยนชื่อให้เป็นมาตรฐาน (Standard Naming)
   - ไฟล์ PDF ประเภท Manual จะถูกแปลงเป็น **Markdown (.md)** อัตโนมัติ (พร้อมใช้งานกับ RAG)
   - จะมีไฟล์ **Metadata (.json)** กำกับข้อมูลไว้ในแต่ละไฟล์

---

## 🧑‍💻 ข้อมูลสำหรับทีมพัฒนา (Developer Guide)

### การเพิ่มหลักสูตรใหม่ (New Course ID)
หากมีหลักสูตรใหม่เพิ่มเติม ให้ไปแก้ไขที่ไฟล์ `scripts/organize_files.py` บริเวณตัวแปร `COURSES_CONTEXT`:

```python
COURSES_CONTEXT = """
หลักสูตรหลักของสถาบันพลังจิตตานุภาพ:
- WP-01: หลักสูตรครูสมาธิ
- WP-XX: ชื่อหลักสูตรใหม่
"""
```

### เกณฑ์การแยกแยะไฟล์ (Classification Logic)
สคริปต์จะอ่านเนื้อหาของไฟล์ 2,000 ตัวอักษรแรก ส่งให้ Gemini 2.0 Flash วิเคราะห์ว่าเป็นประเภทใด:
- **Manual**: คู่มือ, ตำราเรียน, เนื้อหาการสอน
- **Survey**: ผลการสอบ, แบบประเมิน, ความพึงพอใจ, ข้อมูลสำรวจภาคสนาม
- **Exam**: ข้อสอบ, แบบทดสอบ

---
*จัดทำโดย Antigravity AI*
